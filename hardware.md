# ハードウェア構成と接続・ノイズ対策 (hardware.md)

stenobot は AMラジオの音声を Mac mini に入力して録音します。このドキュメントでは、
動作実績のある機材構成・接続方法・ノイズ低減の勘所をまとめます。

> ここで挙げる機材は一例です。同等の機能を持つものなら代替できます。重要なのは
> 「電源を分離する」「アナログ経路のグランドループを断つ」「入力品質を確保する」の3点です。

---

## 接続経路の全体像

```
[AMラジオ]                     SAD-7229/W（ACアダプターでUSB給電・Macと電源系統を分離）
  │ イヤホン（ヘッドホン）出力 3.5mm
  ▼
[グランドループアイソレーター]  Smof ノイズフィルター（アナログ経路のハムを除去）
  │ 3.5mm ライン
  ▼
[USBオーディオIF]              Creative Sound Blaster Play! 3（SB-PLAY3）
  │ USB
  ▼
[Mac mini]                     avfoundation 経由で録音（ffmpeg）
```

ポイントは、**ラジオの給電とMacの給電を別系統にする**ことと、**ラジオとMacのあいだの
アナログ音声経路にグランドループアイソレーターを挟む**ことです。この2つでノイズの大半を抑えられます。

---

## 用意する機材

### 1. AMラジオ（音声出力端子のあるもの）

- 例: **コイズミ SAD-7229/W**（AM/FM ワイドFM対応、ヘッドホン端子あり、USB Type-C充電/乾電池切替式、実用最大出力 約130mW）
- 必須条件: **イヤホン/ヘッドホン端子（3.5mm）があること**。ここから音声を取り出します。
- あると良い: 動かせるアンテナ、電源の切替（USB給電/乾電池）。給電方式を切り替えられると
  ノイズの切り分けに役立ちます（後述）。

### 2. ACアダプター（ラジオ給電用）

- ラジオをUSB給電する場合、**Mac mini とは別系統のACアダプター**から給電します。
- スイッチング式の安価なアダプターはノイズを出す個体があります。**複数試して、最もノイズの
  少ないものを選ぶ**と良い結果が得られます。

### 3. グランドループアイソレーター（アナログ・必須級）

- 例: **Smof ノイズフィルター（グランドループアイソレーター）**
- 役割: ラジオとMacのグランド電位差で生じる「ブーン」というハム（グランドループノイズ）を、
  トランスで絶縁して除去します。アナログのライン/イヤホン経路に挟みます。
- これが効くと、ハムノイズはほぼ解消されます。

### 4. USBオーディオインターフェース

- 例: **Creative Sound Blaster Play! 3（SB-PLAY3）**（最大24bit/96kHz）
- 役割: アナログ音声をデジタル化して Mac にUSB入力します。Mac の内蔵入力が無い/質を上げたい
  場合に使います。
- stenobot は録音時に 16kHz mono へ変換します（whisper.cpp が16kHz前提のため）。AMラジオ音声には
  十分すぎる品質で、ハイレゾ性能は使い切りませんが、安定動作するIFであれば問題ありません。

### 5. Mac mini（録音ホスト）

- macOS の CoreAudio 経由（`sox`）で録音します。常時稼働を想定するため、`pmset` で
  スリープを無効化しておきます。

```bash
sudo pmset -a sleep 0 disksleep 0
```

---

## 接続手順

1. ラジオのヘッドホン端子（3.5mm）に、グランドループアイソレーター（Smof）の入力を挿します。
2. アイソレーターの出力を、USBオーディオIF（SB-PLAY3）のライン/マイク入力に挿します。
3. USBオーディオIFを Mac mini のUSBポートに接続します。
4. ラジオの給電は、**Mac とは別の**ACアダプターから取ります（USB給電の場合）。
5. **システム設定 → サウンド → 入力**で、このUSBオーディオIFを選択します。
   stenobot はデフォルト入力デバイスから録音するため、この設定が録音対象を決めます
   （USB機器の抜き挿しやスリープ復帰で変わることがあるので、録音が想定と違うときは
   まずここを確認します）。

---

## ノイズ低減のために気をつけること

ノイズは「音の種類」で原因が変わります。まず種類を聞き分けると対策が絞れます。

### 「ブーン」という低い連続音（ハム / グランドループ）

- 主因: ラジオとMacのグランドがアナログ経路とUSB経路の双方で繋がり、ループが生じている。
- 対策:
  - **グランドループアイソレーター（Smof）をアナログ経路に挿す**。これが最も効きます。
  - ラジオの給電を **Macと別系統のACアダプター**にして、電源側のループを断つ。

### 「サー」「ジー」「ピー」という高い音（受信状態 / 電源ノイズ / デジタル機器の近接）

- 主因: AM放送はデジタル機器やスイッチング電源のノイズを拾いやすい。Mac mini やそのアダプター、
  USB機器、Wi-Fiルーターの近くにラジオがあると顕著。
- 対策:
  - **ラジオを Mac mini や電源から物理的に離す**。AMアンテナはノイズ源から遠ざける。
  - ラジオの向き・置き方（特にAMはバーアンテナの向き）で受信を最適化する。
  - 良質なACアダプターを選ぶ（個体差が大きいので複数比較）。
  - ケーブルに**フェライトコア（パッチンコア）**を付けると高周波ノイズに効くことがある。

### 入力レベルの最適化

- ラジオの音量とMac/IF側の入力ゲインのバランスを取る。入力が小さすぎてゲインを上げると、
  相対的にノイズが目立ちます。歪まない範囲で適正レベルに調整します。

### 電源を分ける（基本かつ重要）

- ラジオとMacの給電を別系統にするだけでループ起因のノイズを大きく減らせます。USB給電は
  Macやそのハブからではなく、独立したACアダプターから取るのが基本です。

### 切り分けのコツ（USB給電が原因か確かめる）

- ラジオを **一時的に乾電池駆動** に切り替えて録音してみます。これでノイズが消える/激減するなら、
  原因はUSB給電（電源ループまたはACアダプターのノイズ）と確定できます。常時運用では乾電池は
  非現実的なので、原因が分かったら「良質なACアダプター + 電源系統の分離」で対処します。

---

## USBアイソレーターについての注意（重要）

ノイズ対策として「USBアイソレーター」を検討する場合、**オーディオ用途での製品選びに注意**が必要です。

- **ADUM3160 系の USBアイソレーター（例: DSD TECH SH-G01A）は、オーディオIFには使えません。**
  これらは USB Full Speed（12Mbps）までしか通さず、メーカー自身が「オーディオノイズの遮断には
  使わないこと」「オーディオコーデックやDACは非互換」と明記しています。USB Audio Class 2.0 の
  オーディオIF（SB-PLAY3 など高速USB機器）の手前に挟むと、認識不良や動作不良の原因になります。
- そもそも、ラジオのUSBが**給電のみ**でデータ通信していない場合、USBデータを絶縁する
  アイソレーターを挟む意味はありません。
- USB由来のノイズを断ちたいなら、USBアイソレーターより先に「**ラジオを別電源にする**」
  「**アナログ経路にグランドループアイソレーターを入れる**」方が確実かつ安価です。
- どうしてもUSBオーディオ経路を絶縁したい場合は、**USB2.0 High Speed（480Mbps）対応の
  オーディオ用アイソレーター**を選びます（ADUM3160 の12Mbps品とは別物）。

---

## 入力デバイスの設定と確認（v1.3）

USBオーディオIFは機種ごとに固有のサンプルレートを持ちます（SB-PLAY3 等は 44100 または
48000 Hz が一般的）。stenobot は **48000 Hz ステレオ**で取り込み、16kHzモノラルへの変換は
後段の `ffmpeg` が行います。

取り込みは `sox`（CoreAudio）が担当します。sox の CoreAudio ドライバは**デバイス名の指定が
効かない**ため（`'Sound Blaster Play! 3'` を指定すると
`can not get audio device properties` で失敗）、**システムのデフォルト入力デバイス**から
録音します。

**システム設定 → サウンド → 入力**で、使用するオーディオIFを選択してください。ここが
別のデバイス（内蔵マイク、仮想オーディオドライバ等）になっていると、そちらを録ってしまいます。
USBの抜き挿しやスリープ復帰で変わることがあるため、録音が想定と違うときはまずここを疑います。

```bash
# デバイス一覧（参考。sox では番号指定できないが、接続確認には使える）
ffmpeg -f avfoundation -list_devices true -i ""

# デフォルト入力デバイスから3秒録って、レートと音量を確認
rec -c 2 -r 48000 /tmp/test.wav trim 0 3
ffmpeg -i /tmp/test.wav -af volumedetect -f null - 2>&1 | grep -E "mean_volume|max_volume"
#   mean_volume が -91dB 前後 → 無音。デバイス選択かマイク権限を確認
#   mean_volume が -30dB 前後 → 正常
```

> **`ffmpeg -f avfoundation` で録音してはいけません。**
> 常にサンプルの15〜19%を落とします（同一デバイスで sox は100%取得）。v1.1 / v1.2 で
> 「サンプルレート不一致」と説明していた症状の真因はこれでした。詳細は
> [README のトラブルシュート](README.md#録音が60分に満たない場合)を参照してください。

> **入力レベルの目安**
> `mean_volume` が -30dB 前後、`max_volume` が -12dB 前後になるよう、macOS の入力ゲインと
> ラジオ側の音量で調整します。max_volume が 0dB に近いとクリップ（歪み）し、文字起こしの
> 精度が落ちます。

---

## まとめ

- 電源分離（良質なACアダプターをMacと別系統で）+ アナログのグランドループアイソレーター（Smof）
  + 安定したUSBオーディオIF（SB-PLAY3）、の3点で、AMラジオ録音のノイズは実用上十分に抑えられます。
- ノイズが残る場合は「音の種類」で原因を切り分け、低音ハムはグランドループ/電源、高音ノイズは
  受信状態/機器の近接、という方針で対処します。
- USBアイソレーターは用途を誤ると逆効果になるため、オーディオIFには高速対応品のみを使い、
  まずは電源分離とアナログ側の対策を優先します。
